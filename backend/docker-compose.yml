version: '3.8'
services:
  backend:
    build: .
    container_name: webseccheck-backend
    restart: unless-stopped
    ports:
      - '3000:7860'
    volumes:
      - ./data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
